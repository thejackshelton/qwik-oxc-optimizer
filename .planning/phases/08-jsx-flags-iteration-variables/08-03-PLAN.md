---
phase: 08-jsx-flags-iteration-variables
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/jsx_transform.rs
autonomous: true

must_haves:
  truths:
    - "SWC=2/OXC=3 flag mismatches investigated: either resolved by 08-01+02 scope/q:p fixes, or targeted fixes applied"
    - "Remaining flag mismatches after 08-01+02 are documented with root causes and fix status"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/jsx_transform.rs"
      provides: "Any targeted static_listeners fixes for non-q:p SWC=2 cases"
      contains: "static_listeners"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/jsx_transform.rs"
      to: "crates/qwik-optimizer-oxc/tests/snapshots/"
      via: "Flag value computation verified against snapshot diffs"
      pattern: "static_listeners|static_subtree"
---

<objective>
Investigate and fix remaining SWC=2/OXC=3 flag mismatches (static_listeners should be false but is true) that may not be covered by 08-01's identifier scope fix or 08-02's q:p/static_listeners fix.

Purpose: Phase 8 success criterion #2 requires eliminating "29 OXC=3/SWC=1 mismatches" related to logical && propagation. Research shows these break down into: (a) 13 SWC=1/OXC=3 children cases -- handled by 08-01's scope-aware is_child_expression_immutable; (b) 9 SWC=2/OXC=3 cases -- these involve static_listeners being false in SWC, possibly related to _qrlSync or other special cases beyond q:p. After 08-01 and 08-02 execute, some or all of these 9 cases may self-resolve. This plan audits the remaining state and applies targeted fixes for any that persist.

Output: All SWC=2/OXC=3 cases either confirmed resolved by prior plans or fixed with targeted changes. Comprehensive audit of remaining flag mismatches documented in SUMMARY.
</objective>

<execution_context>
@/Users/maieul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maieul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-jsx-flags-iteration-variables/08-RESEARCH.md
@.planning/phases/08-jsx-flags-iteration-variables/08-01-SUMMARY.md
@.planning/phases/08-jsx-flags-iteration-variables/08-02-SUMMARY.md
@crates/qwik-optimizer-oxc/src/jsx_transform.rs
@crates/qwik-optimizer-oxc/src/transform.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit remaining flag mismatches post-08-01/08-02 and fix SWC=2/OXC=3 cases</name>
  <files>crates/qwik-optimizer-oxc/src/jsx_transform.rs, crates/qwik-optimizer-oxc/src/transform.rs</files>
  <action>
1. **Run snapshot diff analysis to identify remaining flag mismatches:**

```bash
cd /Users/maieul/dev/work/qwik-oxc-optimizer/.claude/worktrees/sharp-brown
cargo insta test -p qwik-optimizer-oxc --accept 2>&1 | tail -5
```

Then analyze the remaining diffs for flag mismatches:
```bash
git diff crates/qwik-optimizer-oxc/tests/snapshots/ | grep -B2 -A2 '"flags"'
```

Categorize remaining mismatches into:
- SWC=2/OXC=3 (static_listeners should be false, was true) -- PRIMARY TARGET
- Any other remaining categories

2. **For each SWC=2/OXC=3 case, investigate the specific test:**

Read the test input and trace through the flag computation logic:
- Is there a `_qrlSync` call that should clear static_listeners?
- Is there a q:p case that 08-02 should have caught but didn't?
- Is there a spread or other condition that SWC considers non-static?

The research notes these may involve `_qrlSync` or other special cases. Check if the test files in question contain patterns like:
- `_qrlSync(...)` in event handlers
- Inline event handlers that don't go through the `$` transformation
- Other patterns that SWC treats as non-static listeners

3. **Apply targeted fixes for any remaining SWC=2/OXC=3 cases:**

Based on investigation, likely fixes include:
- If `_qrlSync` triggers static_listeners=false in SWC: add a check in flag computation for _qrlSync presence in const_props/var_props
- If it's a scoping issue: the identifier scope fix from 08-01 may already handle it
- If it's a q:p case that 08-02 missed: investigate why the pre-recorded side-channel didn't capture it

For each fix, ensure it doesn't regress other test cases.

4. **Document all remaining flag mismatches in the SUMMARY:**

Create a table of:
- Test name
- Expected flag (SWC)
- Actual flag (OXC)
- Root cause
- Status (fixed / deferred to Phase 9 / known limitation)

Restore snapshots after analysis:
```bash
git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/
```

IMPORTANT: Do NOT stage or commit any files under `crates/qwik-optimizer-oxc/tests/snapshots/`. Always restore them after testing with `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`.
  </action>
  <verify>
1. `cargo check -p qwik-optimizer-oxc` compiles without errors
2. `cargo insta test -p qwik-optimizer-oxc --accept` then count remaining flag mismatches:
   ```
   git diff crates/qwik-optimizer-oxc/tests/snapshots/ | grep '"flags"' | wc -l
   ```
   Target: <=15 total flag mismatches remaining (combined with 08-01 and 08-02 results)
3. Specifically verify SWC=2/OXC=3 cases: should be 0 remaining or documented with clear root cause
4. Restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
  </verify>
  <done>
- All 9 SWC=2/OXC=3 cases either resolved by prior plans or fixed with targeted changes
- Remaining flag mismatches documented with root causes and fix status
- Total flag mismatches <=15 (phase success criterion #6 met)
- No regressions introduced
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p qwik-optimizer-oxc` compiles cleanly
2. SWC=2/OXC=3 flag mismatch category has 0 remaining cases (or all documented with clear root cause for Phase 9)
3. Total flag mismatches <=15 (down from 108)
4. No snapshot files committed
</verification>

<success_criteria>
- SWC=2/OXC=3 cases fully investigated and addressed (fixed or documented with root cause)
- Comprehensive flag mismatch audit completed and documented in SUMMARY
- Combined with 08-01 and 08-02, phase 8 success criteria met (flag mismatches <=15)
</success_criteria>

<output>
After completion, create `.planning/phases/08-jsx-flags-iteration-variables/08-03-SUMMARY.md`
</output>
