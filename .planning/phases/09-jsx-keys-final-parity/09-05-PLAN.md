---
phase: 09-jsx-keys-final-parity
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
  - crates/qwik-optimizer-oxc/src/jsx_transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/tests/test.rs
autonomous: true

must_haves:
  truths:
    - "relative_paths test produces output matching SWC structure (dep file not re-processed)"
    - "All 162 snapshots match SWC golden reference exactly (0 diffs)"
    - "No regressions from earlier phases"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "All remaining edge case fixes"
    - path: "crates/qwik-optimizer-oxc/tests/test.rs"
      provides: "Test infrastructure fixes if needed"
  key_links:
    - from: "Full test suite"
      to: "Golden snapshots"
      via: "cargo insta test"
      pattern: "0 snapshots to review"
---

<objective>
Final audit and closure: fix the relative_paths multi-input test, address ALL remaining snapshot diffs after plans 01-04, and achieve 0/162 diff target.

Purpose: This is the final sweep. After plans 01-04 address the major categories, this plan catches everything that fell through the cracks and resolves the relative_paths structural issue.
Output: All 162 snapshots match SWC golden reference exactly.
</objective>

<execution_context>
@/Users/maieul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maieul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-jsx-keys-final-parity/09-RESEARCH.md
@.planning/phases/09-jsx-keys-final-parity/09-01-SUMMARY.md
@.planning/phases/09-jsx-keys-final-parity/09-02-SUMMARY.md
@.planning/phases/09-jsx-keys-final-parity/09-03-SUMMARY.md
@.planning/phases/09-jsx-keys-final-parity/09-04-SUMMARY.md
@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/src/code_move.rs
@crates/qwik-optimizer-oxc/src/lib.rs
@crates/qwik-optimizer-oxc/tests/test.rs
@crates/swc-optimizer/core/src/transform.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix relative_paths multi-input test</name>
  <files>
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/lib.rs
    crates/qwik-optimizer-oxc/tests/test.rs
  </files>
  <action>
The `relative_paths` test is a multi-input test with `additional_inputs` containing a pre-compiled dep file that already has `inlinedQrl()` calls. SWC passes this through with minimal changes; OXC re-processes it, extracting segments where it shouldn't.

**Investigation steps:**
1. Read the `relative_paths` test case in test.rs to understand the input structure
2. The dep file (typically in `node_modules/dep/dist/lib.mjs`) already contains `inlinedQrl()` calls
3. SWC's transform detects pre-compiled QRL patterns and skips extraction
4. OXC treats `inlinedQrl()` as a $-like call and tries to extract segments from it

**Fix approach:**
Option A: When processing `additional_inputs`, skip the segment extraction pass entirely — these are pre-compiled modules.
Option B: Detect `inlinedQrl()` calls (and other already-compiled QRL patterns like `componentQrl()`) and skip them during `$()` detection. The dollar-sign detection should only fire on `$()` calls (with the `$` suffix), not on `inlinedQrl()` / `componentQrl()` calls.

Check: Does OXC's `$()` detection (`is_dollar_call` or equivalent) incorrectly match `inlinedQrl()` calls? If the dep file's `inlinedQrl()` calls are being treated as new `$()` boundaries, that's the bug.

Also check: The dep file may use a different module path. The test may configure `additional_inputs` with specific relative paths that affect how the output is structured.

The relative_paths golden snapshot shows the dep file passing through mostly unchanged, with the main file producing `qrl()` references (segment strategy). Make OXC match this structure exactly.
  </action>
  <verify>
Run `cargo insta test -p qwik-optimizer-oxc` and check:
1. `relative_paths` snapshot matches SWC golden reference
2. Dep file output preserved without re-extraction
  </verify>
  <done>
- relative_paths test output matches SWC structure
- Additional input files not re-processed for segment extraction
  </done>
</task>

<task type="auto">
  <name>Task 2: Final audit — close ALL remaining snapshot diffs to 0/162</name>
  <files>
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/code_move.rs
    crates/qwik-optimizer-oxc/src/jsx_transform.rs
    crates/qwik-optimizer-oxc/src/lib.rs
  </files>
  <action>
After plans 01-04 and Task 1 above, run `cargo insta test -p qwik-optimizer-oxc` and enumerate ALL remaining diffs.

For each remaining diff file:
1. Diff the golden snapshot against the new output
2. Categorize the diff (import order, const assignment, capture, flag, key, formatting, etc.)
3. Fix the root cause

Common remaining issues will likely be:
- **assertion_line** diffs: These are insta metadata, not code diffs. If assertion_line is the ONLY diff, it can be resolved by accepting the snapshot.
- **Minor formatting**: Trailing whitespace, newline differences, semicolons
- **Flag value differences**: May need targeted flag computation fixes
- **Key counter skews**: From structural changes earlier in the pipeline

**Approach:**
1. Run tests, count remaining diffs
2. Sort by category (group identical root causes)
3. Fix each root cause (1 fix = N files resolved)
4. Iterate until 0 diffs remain

**Critical check:** After ALL fixes, run `cargo insta test -p qwik-optimizer-oxc` one final time. The output should say "0 snapshots to review" (or all tests pass with no new snapshots).

**WARNING:** Do NOT accept snapshots (--accept). The golden snapshots are the SWC reference. Only the code should change, never the snapshots.

The target is 0/162 diffs. If a particular diff requires an architectural change, implement that change — do not defer it. This task must achieve exact parity.
  </action>
  <verify>
Run `cargo insta test -p qwik-optimizer-oxc 2>&1 | grep "snapshots to review"` — should show "0 snapshots to review" or "info: test run was successful, no changes detected"
  </verify>
  <done>
- All 162 snapshots match SWC golden reference exactly (0 diffs remaining)
  </done>
</task>

</tasks>

<verification>
Final verification:
1. `cargo insta test -p qwik-optimizer-oxc` produces 0 new snapshots
2. `cargo test -p qwik-optimizer-oxc` passes all tests
3. No snapshot files have been modified (git status shows clean snapshots directory)
4. Run `git diff --stat -- crates/qwik-optimizer-oxc/tests/snapshots/` — should show 0 files changed
</verification>

<success_criteria>
- relative_paths test matches SWC
- All 162 snapshots match SWC golden reference (0 diffs)
- Full test suite passes
- Milestone v1.0 (Full Snapshot Parity) achieved
</success_criteria>

<output>
After completion, create `.planning/phases/09-jsx-keys-final-parity/09-05-SUMMARY.md`
</output>
