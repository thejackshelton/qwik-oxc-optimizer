---
phase: 14-final-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/qwik-optimizer-oxc/src/transform.rs
autonomous: true

must_haves:
  truths:
    - "Entry module import ordering matches SWC encounter order across all tests"
    - "_Fragment import appears AFTER hoisted stmts and lazy imports, not sorted with framework imports"
    - "Lazy import declarations appear between synthetic framework imports and non-dollar Qwik imports"
    - "No alphabetical sorting of synthetic imports -- insertion order preserved"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "Restructured exit_program import assembly matching SWC encounter order"
      contains: "Phase 4"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "tests/snapshots/*.snap"
      via: "cargo insta test --accept"
      pattern: "synthetic_imports"
---

<objective>
Fix entry module import ordering to match SWC's encounter-order pattern instead of alphabetical sorting.

Purpose: Import ordering is the single largest category of remaining diffs (44 tests). SWC uses a BTreeMap<Id> where Id sorts by (SyntaxContext, JsWord), and SyntaxContext values increase in encounter order -- so the effective output order follows the traversal encounter order, not alphabetical. The current OXC approach splits imports into qrl_group and framework_group, sorts each alphabetically, then chains them. This produces wrong ordering.

Output: Modified `exit_program` in transform.rs with correct import assembly order.
</objective>

<execution_context>
@/Users/maieul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maieul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-final-parity/14-RESEARCH.md
@crates/qwik-optimizer-oxc/src/transform.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure entry module import ordering to match SWC encounter order</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs</files>
  <action>
Modify the `exit_program` function (around lines 3600-3943) to fix the import ordering:

1. **Remove alphabetical sorting** (lines 3762-3773): Delete the partition into qrl_group/framework_group and the two `.sort_by()` calls. Instead, keep synthetic_imports in insertion order.

2. **Separate _Fragment from the synthetic_imports list**: Currently _Fragment is added at the end of the if-chain (line 3724). Instead, REMOVE the _Fragment push from the synthetic_imports list. It will be emitted separately in Phase 7 assembly, AFTER lazy imports.

3. **Restructure Phase 7 assembly order** (lines 3875-3943) to match SWC:
   - Step 1: Filtered synthetic framework imports (in insertion/encounter order, WITHOUT _Fragment)
   - Step 2: Lazy import declarations (const i_XXX = ...)
   - Step 3: _Fragment import (from jsx-runtime) -- build it here, AFTER lazy imports
   - Step 4: Non-dollar Qwik core specifiers (useStore, mutable, etc.)
   - Step 5: Original non-Qwik imports (filtered)
   - Step 6: Non-import code
   - Step 7: _auto_ exports

4. **Build _Fragment import at assembly time**: In the Phase 7 section, check `self.import_tracker.needs_fragment`. If true, build the _Fragment import statement using `import_rewrite::build_aliased_import("Fragment", "_Fragment", "@qwik.dev/core/jsx-runtime", ctx)` and insert it at the correct position (after lazy imports, before non-dollar imports).

5. **Keep the referenced_idents filtering** (line 3778): The filter that removes unreferenced synthetic imports from the entry module is still needed and correct.

The key insight: SWC's encounter order for a typical component is:
- componentQrl (first $-call encountered)
- _wrapProp, _fnSignal (from signal wrapping during JSX processing)
- _jsxSorted (from JSX element processing)
- inlinedQrl (from nested $-calls)
- _captures, _restProps (from capture processing)
- qrl/qrlDEV (from segment extraction)
- Other utilities (_val, _chk, _noopQrl, _qrlSync)

The current if-chain in Phase 3 (lines 3618-3725) already adds imports in approximately this order, so removing the sort and extracting _Fragment should be sufficient for most tests.

After making changes, run: `cargo insta test --accept -p qwik-optimizer-oxc`
Then restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
Then run comparison to count improvements: `cargo insta test -p qwik-optimizer-oxc 2>&1 | tail -5`

If specific tests still have wrong order, the if-chain order in Phase 3 may need reordering. Check the specific diffs and adjust accordingly. The encounter order is:
1. Qrl-suffixed user imports (componentQrl, etc.) -- these DO stay first
2. qrl/qrlDEV, inlinedQrl/inlinedQrlDEV
3. _captures, _restProps
4. _jsxSorted/_jsx
5. _getVarProps, _getConstProps, _jsxSplit
6. _wrapProp, _fnSignal, _val, _chk
7. _noopQrl/_noopQrlDEV, _qrlSync
8. (lazy imports go here)
9. _Fragment (AFTER lazy imports)
10. Non-dollar Qwik core (useStore, etc.)

If the if-chain doesn't produce this order, reorder the if-blocks. The current order (lines 3618-3725) is close but may need `_wrapProp`/`_fnSignal` moved before `_jsxSorted`.
  </action>
  <verify>
Run `cargo insta test -p qwik-optimizer-oxc 2>&1 | grep -E "test result|snapshot"` to see test results.
Then check specific import-order-only tests:
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_derived_signals_children.snap crates/qwik-optimizer-oxc/tests/snapshots/example_derived_signals_children.snap.new`
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_jsx.snap crates/qwik-optimizer-oxc/tests/snapshots/example_jsx.snap.new`
- `diff crates/qwik-optimizer-oxc/tests/snapshots/ternary_prop.snap crates/qwik-optimizer-oxc/tests/snapshots/ternary_prop.snap.new`
These should show NO diff (exact match) since they only had IMPORT_ORDER diffs.
Always restore snapshots after testing: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
  </verify>
  <done>
Import ordering in entry modules matches SWC across all tests. The tests that had ONLY IMPORT_ORDER diffs now produce exact matches. No regressions in existing exact-match tests.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` passes (no panics)
2. Tests with only IMPORT_ORDER category become exact matches: example_derived_signals_children, example_derived_signals_multiple_children, example_issue_4438, example_jsx, ternary_prop
3. No regressions in the 72 existing exact-match tests
4. Always restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
</verification>

<success_criteria>
- Import ordering matches SWC encounter order (not alphabetical)
- _Fragment appears after lazy imports in entry module output
- 5+ new exact matches from import-order-only tests
- Zero regressions in existing exact matches
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-parity/14-01-SUMMARY.md`
</output>
