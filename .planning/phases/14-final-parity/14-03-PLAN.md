---
phase: 14-final-parity
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/types.rs
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/tests/test.rs
autonomous: true

must_haves:
  truths:
    - "Segment strategy entry field is 'entry_segments' (not null)"
    - "Smart/Component strategy entry field includes stack context names"
    - "preserve_filenames=true keeps original file extension in output path"
    - "Dev mode tests use src_dir=/user/qwik/src/ matching SWC test defaults"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/lib.rs"
      provides: "Fixed compute_entry_field for Segment strategy, preserve_filenames extension skip, stack_ctxt threading"
      contains: "entry_segments"
    - path: "crates/qwik-optimizer-oxc/src/types.rs"
      provides: "stack_ctxt field on SegmentData"
      contains: "stack_ctxt"
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "stack_ctxt populated at SegmentData construction sites"
      contains: "stack_ctxt"
    - path: "crates/qwik-optimizer-oxc/tests/test.rs"
      provides: "Fixed dev mode test src_dir defaults"
      contains: "/user/qwik/src/"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/lib.rs"
      to: "tests/snapshots/example_11.snap"
      via: "compute_entry_field returns Some for Segment"
      pattern: "entry_segments"
---

<objective>
Fix entry field metadata, file extension handling, and dev mode test configuration.

Purpose: Three independent small fixes that together affect ~10 tests: (1) entry field returns null instead of "entry_segments" for Segment strategy (4 tests), (2) preserve_filenames doesn't prevent extension transformation (2 tests), (3) dev mode test src_dir mismatch (4 tests). All are simple, well-understood fixes.

Output: Modified lib.rs (entry field + file extension), types.rs (stack_ctxt on SegmentData), transform.rs (stack_ctxt population at SegmentData construction sites), test.rs (src_dir defaults).
</objective>

<execution_context>
@/Users/maieul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maieul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-final-parity/14-RESEARCH.md
@crates/qwik-optimizer-oxc/src/lib.rs
@crates/qwik-optimizer-oxc/src/types.rs
@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/tests/test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix entry field for Segment strategy + thread stack_ctxt for Smart/Component</name>
  <files>crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/src/types.rs, crates/qwik-optimizer-oxc/src/transform.rs</files>
  <action>
Three changes needed:

**1. Fix compute_entry_field for Segment/Hook strategies (lib.rs ~line 422):**

Change:
```rust
EntryStrategy::Segment | EntryStrategy::Hook => None,
```
To:
```rust
EntryStrategy::Segment | EntryStrategy::Hook => Some("entry_segments".to_string()),
```

This matches the golden snapshots which show `"entry": "entry_segments"` for Segment strategy tests (example_11, etc.).

**2. Add stack_ctxt to SegmentData (types.rs ~line 507):**

Add a new field to the `SegmentData` struct:
```rust
/// Stack context names at the time of segment creation.
/// Used by Smart/Component entry strategies to compute entry field.
pub stack_ctxt: Vec<String>,
```

**3. Thread stack_ctxt through segment creation (transform.rs):**

In `record_dollar_call_segment` and `record_jsx_event_segment`, when constructing `SegmentData`, populate `stack_ctxt: self.stack_ctxt.clone()`.

NOTE: This modifies transform.rs ONLY at SegmentData construction sites (~lines 960 and 1059), NOT the exit_program function that Plan 01 modifies. These are completely different functions with no overlap.

**4. Use stack_ctxt in segment_data_to_analysis (lib.rs ~line 458):**

Change:
```rust
let entry = compute_entry_field(
    entry_strategy,
    &normalized_origin,
    &[],  // <-- was empty
);
```
To:
```rust
let entry = compute_entry_field(
    entry_strategy,
    &normalized_origin,
    &seg.stack_ctxt,  // <-- use actual stack context
);
```

This fixes Smart strategy tests (example_default_export, example_manual_chunks, example_use_server_mount) which need `"{origin}_entry_{root}"` format.

After making changes, run: `cargo insta test --accept -p qwik-optimizer-oxc`
Then restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
Then check entry field diffs are resolved.
  </action>
  <verify>
Run `cargo insta test --accept -p qwik-optimizer-oxc` then check:
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_11.snap crates/qwik-optimizer-oxc/tests/snapshots/example_11.snap.new` -- entry field diff gone
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_default_export.snap crates/qwik-optimizer-oxc/tests/snapshots/example_default_export.snap.new` -- entry field diff gone
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_manual_chunks.snap crates/qwik-optimizer-oxc/tests/snapshots/example_manual_chunks.snap.new` -- entry field diff gone
Always restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
  </verify>
  <done>Entry field metadata matches SWC for Segment, Smart, and Component strategies across all 4 affected tests.</done>
</task>

<task type="auto">
  <name>Task 2: Fix preserve_filenames extension + dev mode src_dir</name>
  <files>crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/tests/test.rs</files>
  <action>
Two independent fixes:

**1. Fix preserve_filenames extension handling (lib.rs ~line 222):**

Change:
```rust
let main_path = if transform_options.transpile_ts || transform_options.transpile_jsx {
```
To:
```rust
let main_path = if (transform_options.transpile_ts || transform_options.transpile_jsx)
    && !transform_options.preserve_filenames {
```

This makes the main module output keep the original file extension (e.g., `test.tsx` not `test.ts` or `test.js`) when preserve_filenames is true.

Golden snapshots show:
- `example_preserve_filenames`: output path should be `test.tsx` (transpile_ts=true, transpile_jsx=true, preserve_filenames=true)
- `example_preserve_filenames_segments`: output path should be `test.tsx` (transpile_ts=true, transpile_jsx=true, preserve_filenames=true)

**2. Fix dev mode test src_dir defaults (tests/test.rs):**

In the `apply_case_overrides` function, find the dev mode test cases and add `src_dir` override. SWC test defaults use `"/user/qwik/src/"` for dev mode tests, while OXC defaults to `"."`.

Add `case.src_dir = "/user/qwik/src/".to_string();` to these test case overrides:
- `"example_dev_mode"` (around line 846)
- `"example_dev_mode_inlined"` (around line 851)
- `"example_drop_side_effects"` (around line 857)
- `"example_noop_dev_mode"` (around line 985)

This changes the dev_abs_path from `"./test.tsx"` to `"/user/qwik/src/test.tsx"` matching SWC golden output.

After both fixes, run: `cargo insta test --accept -p qwik-optimizer-oxc`
Then restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
  </action>
  <verify>
Run `cargo insta test --accept -p qwik-optimizer-oxc` then check:
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_preserve_filenames.snap crates/qwik-optimizer-oxc/tests/snapshots/example_preserve_filenames.snap.new` -- extension diff gone (test.tsx not test.ts)
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_preserve_filenames_segments.snap crates/qwik-optimizer-oxc/tests/snapshots/example_preserve_filenames_segments.snap.new` -- extension diff gone (test.tsx not test.js)
- `diff crates/qwik-optimizer-oxc/tests/snapshots/example_dev_mode.snap crates/qwik-optimizer-oxc/tests/snapshots/example_dev_mode.snap.new` -- dev path diff reduced
Always restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
  </verify>
  <done>preserve_filenames keeps original extension. Dev mode tests use correct src_dir matching SWC defaults. 2 FILE_EXT diffs resolved, 4 DEV_MODE path diffs resolved.</done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` passes (no panics)
2. Entry-field-only tests become exact matches: example_11, example_default_export
3. File extension diffs resolved for: example_preserve_filenames, example_preserve_filenames_segments
4. Dev mode path diffs resolved for: example_dev_mode, example_dev_mode_inlined, example_drop_side_effects, example_noop_dev_mode
5. No regressions in existing exact-match tests
6. Always restore snapshots: `git checkout -- crates/qwik-optimizer-oxc/tests/snapshots/`
</verification>

<success_criteria>
- Entry field "entry_segments" for Segment strategy
- Smart/Component strategies use stack_ctxt for entry names
- preserve_filenames skips extension transformation
- Dev mode tests use /user/qwik/src/ src_dir
- 2+ new exact matches from entry-field-only tests
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-parity/14-03-SUMMARY.md`
</output>
