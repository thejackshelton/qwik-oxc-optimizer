{
  "name": "qwik-oxc-optimizer-tools",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "bun-types": "^1.3.11",
    "oxfmt": "0.32.0"
  },
  "scripts": {
    "test": "bun test",
    "harness": "bun src/cli.ts --swc-snapshots swc-snapshots/",
    "fmt:snapshot-blocks": "bun scripts/format-snapshot-code-blocks.mjs",
    "fmt:snapshot-blocks:check": "bun scripts/format-snapshot-code-blocks.mjs --check",
    "oxfmt:version": "oxfmt --version",
    "sync:snapshots": "node scripts/sync-upstream-snapshots.mjs",
    "sync:snapshots:dry": "node scripts/sync-upstream-snapshots.mjs --dry-run"
  },
  "dependencies": {
    "siphash": "^1.2.0"
  }
}
